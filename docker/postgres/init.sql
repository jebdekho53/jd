-- PostgreSQL extensions for Jebdekho
CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- PostGIS (uncomment for Phase 2 geospatial upgrade)
-- CREATE EXTENSION IF NOT EXISTS postgis;

-- Full-text search config
-- CREATE TEXT SEARCH CONFIGURATION IF NOT EXISTS jebdekho_search ( COPY = english );
