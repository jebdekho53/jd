import { CategoryCatalogKind } from '@prisma/client';
import { PrismaService } from '../../database/prisma.service';
import { AuditService } from '../audit/audit.service';
import { DomainEventsService } from '../domain-events/domain-events.service';
import { MerchantService } from '../merchant/merchant.service';
import { VerificationBlocklistService } from '../merchant/verification-blocklist.service';
import { RequestStoreCategoryAccessDto, UploadCategoryDocumentDto } from './dto/category-governance.dto';
import { StoreCategoryAccessService } from './store-category-access.service';
import { ConfigService } from '@nestjs/config';
import { VerticalService } from '../store-vertical/vertical.service';
export declare class StoreCategoryRequestService {
    private readonly prisma;
    private readonly merchantService;
    private readonly blocklist;
    private readonly audit;
    private readonly domainEvents;
    private readonly categoryAccess;
    private readonly config;
    private readonly verticalService;
    private readonly logger;
    constructor(prisma: PrismaService, merchantService: MerchantService, blocklist: VerificationBlocklistService, audit: AuditService, domainEvents: DomainEventsService, categoryAccess: StoreCategoryAccessService, config: ConfigService, verticalService: VerticalService);
    private assertStoreOwned;
    listCatalog(userId: string, storeId: string, catalogKind?: CategoryCatalogKind): Promise<any>;
    listStoreRequests(userId: string, storeId: string): Promise<any>;
    listApprovedCategories(userId: string, storeId: string, catalogKind?: CategoryCatalogKind): Promise<import("./store-category-access.service").ApprovedCategoryTree[]>;
    requestCategoryAccess(userId: string, storeId: string, dto: RequestStoreCategoryAccessDto, ipAddress?: string): Promise<any>;
    uploadDocument(userId: string, storeId: string, requestId: string, dto: UploadCategoryDocumentDto, ipAddress?: string): Promise<any>;
    submitDocuments(userId: string, storeId: string, requestId: string, ipAddress?: string): Promise<any>;
    private emitEvent;
}
